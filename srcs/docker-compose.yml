version: '3'

services:

  mariadb:
    build: ./requirements/mariadb/
    image: mydb:inception
    container_name: mariadb
    env_file: ".env"
    expose:
      - ${MDB_EXPOSE}
    volumes:
      - ~/home/sap/data
    restart: always
